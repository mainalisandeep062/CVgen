<?xml version="1.0" encoding="UTF-8" ?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.29.xsd">

    <changeSet id="003-create-refresh-tokens-table" author="sandeep mainali">
        <createTable tableName="refresh_tokens">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="user_id" type="UUID">
                <constraints nullable="false" references="users(id)" foreignKeyName="fk_refresh_token_user"
                             deleteCascade="true"/>
            </column>

            <column name="family_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <!-- JWT `jti` of the issued token; the lookup key used on refresh. -->
            <column name="jti" type="UUID">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_refresh_tokens_jti"/>
            </column>

            <column name="token_hash" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="expires_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="revocation_reason" type="VARCHAR(50)"/>

            <column name="revoked_at" type="TIMESTAMP WITH TIME ZONE"/>

            <column name="replaced_by_id" type="UUID"/>

            <column name="last_used_at" type="TIMESTAMP WITH TIME ZONE"/>

            <column name="created_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex tableName="refresh_tokens" indexName="idx_refresh_tokens_user_id">
            <column name="user_id"/>
        </createIndex>

        <addForeignKeyConstraint baseTableName="refresh_tokens"
                                 baseColumnNames="replaced_by_id"
                                 referencedTableName="refresh_tokens"
                                 referencedColumnNames="id"
                                 constraintName="fk_refresh_token_replaced_by"
                                 onDelete="SET NULL"/>


    </changeSet>
</databaseChangeLog>