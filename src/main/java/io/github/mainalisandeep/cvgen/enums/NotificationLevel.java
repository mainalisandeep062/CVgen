package io.github.mainalisandeep.cvgen.enums;

/** How prominently the frontend presents a notification. Presentation only. */
public enum NotificationLevel {
    INFO,
    SUCCESS,
    WARNING,
    CRITICAL
}
