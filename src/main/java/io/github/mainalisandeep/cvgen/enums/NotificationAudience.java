package io.github.mainalisandeep.cvgen.enums;

/**
 * Who a notification is for.
 */
public enum NotificationAudience {

    /** Broadcast: visible to every account that existed when it was sent. */
    ALL,

    /** Addressed to exactly one recipient. */
    USER
}
