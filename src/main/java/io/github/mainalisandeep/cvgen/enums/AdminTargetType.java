package io.github.mainalisandeep.cvgen.enums;

/** Kind of row an audit entry's {@code target_id} points at. */
public enum AdminTargetType {
    USER,
    TEMPLATE,
    CREDIT_PACK,
    TRANSACTION,
    NOTIFICATION
}
