package io.github.mainalisandeep.cvgen.enums;

/** Kind of entry in the admin dashboard's recent-activity feed. */
public enum ActivityType {
    USER_SIGNUP,
    CV_CREATED,
    PURCHASE,
    ADMIN_ACTION
}
